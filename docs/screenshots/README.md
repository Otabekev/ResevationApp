# Screenshots

Add product screenshots here and embed them in the main [`README`](../../README.md).

The three that make the strongest impression on reviewers:

1. **`dashboard.png`** — the owner dashboard (bookings / today view).
2. **`bot-booking.png`** — the customer booking flow inside Telegram.
3. **`queue.png`** — the live queue (ticket with position + ETA, or the desk view).

Tips: use a phone frame for the bot shots, crop tight, keep them under ~500 KB each.
