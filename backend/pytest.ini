[pytest]
asyncio_mode = auto
asyncio_default_fixture_loop_scope = function
pythonpath = .
testpaths = tests
addopts = -q
filterwarnings =
    ignore::DeprecationWarning
markers =
    postgres: test requires a real PostgreSQL database (set TEST_DATABASE_URL); skipped otherwise
