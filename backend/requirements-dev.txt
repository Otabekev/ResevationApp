# Test/dev-only dependencies. Install with:
#   pip install -r requirements.txt -r requirements-dev.txt
-r requirements.txt
pytest==8.3.4
pytest-asyncio==0.24.0
aiosqlite==0.20.0      # async SQLite driver used as the test database
